line-length = 110
target-version = "py312"

[lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]  # long lines tolerated in this codebase

[lint.per-file-ignores]
"src/*" = ["E402"]  # sys.path.insert before imports is intentional
